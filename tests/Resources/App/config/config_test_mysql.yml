imports:
    - { resource: config.yml }

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_mysql
        host: 127.0.0.1
        port: null
        dbname: blast_test
        user: root
        password: root
        charset: UTF8
    orm:
        auto_mapping: true

