imports:
    - { resource: config.yml }

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_pgsql
        host: 127.0.0.1
        port: 5432
        dbname: blast_test
        user: blast_user_test
        password: blast_password_test
        charset: UTF8
    orm:
        auto_mapping: true
